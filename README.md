

```markdown
# EnergyMap

Проект для мониторинга и визуализации данных энергетики (в разработке).

## Текущая версия
- Django 6.0.2  
- MySQL 8.4  
- Docker + Docker Compose

## Быстрый запуск (на любом компьютере)

### Требования
- Установлен **Docker Desktop** → https://www.docker.com/products/docker-desktop/
- Git

### Запуск проекта

1. Склонируйте репозиторий:
   ```bash
   git clone https://github.com/stasevboris/energymap.git
   cd energymap
   ```

2. Соберите и запустите контейнеры:
   ```bash
   docker-compose up --build
   ```

   - Первый запуск может занять 5–10 минут (скачивание образов, установка зависимостей)
   - После запуска откройте в браузере: http://localhost:8000

3. Остановить проект:
   Нажмите `Ctrl + C` в терминале  
   или в отдельном окне:
   ```bash
   docker-compose down
   ```

### Полезные команды внутри Docker

- Применить миграции (создать/обновить таблицы в БД):
  ```bash
  docker-compose exec web python manage.py migrate
  ```

- Создать суперпользователя для админки (/admin/):
  ```bash
  docker-compose exec web python manage.py createsuperuser
  ```

- Зайти в bash контейнера Django (для отладки, shell и т.д.):
  ```bash
  docker-compose exec web bash
  ```

- Перезапустить с пересборкой (после изменения requirements.txt, Dockerfile):
  ```bash
  docker-compose up --build
  ```

- Очистить базу данных и начать с нуля:
  ```bash
  docker-compose down -v
  docker-compose up --build
  ```

### Как вносить изменения в код

1. Откройте проект в любом редакторе (PyCharm, VS Code и т.д.)
2. Внесите изменения (например в `hello/views.py`)
3. Сохраните файл → изменения сразу видны на http://localhost:8000  
   (благодаря монтированию volume — hot-reload работает)
4. После тестирования закоммитьте и запушьте:

   ```bash
   git add .
   git commit -m "Добавлена новая функция / страница"
   git push origin main
   ```

### Настройка стиля кода (обязательно для всех разработчиков)

Проект использует **pre-commit** + **black** + **isort** для автоматического форматирования кода.

После клонирования репозитория **один раз** выполните:

```bash
pip install pre-commit
pre-commit install
```

Теперь перед каждым коммитом код будет автоматически отформатирован (black + isort).  
Это гарантирует, что diff'ы чистые и стиль одинаковый у всех участников.

### Структура проекта (основные файлы и папки)

```
energymap/
├── energymap/          # настройки проекта Django (settings.py, urls.py и т.д.)
├── hello/              # тестовое приложение с привет-мир
├── Dockerfile          # сборка образа Python + Django
├── docker-compose.yml  # описание сервисов (web + db)
├── requirements.txt    # зависимости Python
├── .gitignore
└── README.md           # этот файл
```

### Разработка в команде

- Работайте в отдельных ветках:
  ```bash
  git checkout -b feature/название-ветки
  ```
- После завершения — создайте Pull Request в `main`
- Не коммитьте личные файлы редакторов (.idea/, .vscode/, *.code-workspace)

### Возможные проблемы и решения

- **"Can't connect to MySQL server on 'db'"** → подождите 30–60 секунд после запуска, MySQL инициализируется медленно первый раз. Перезапустите `docker-compose up`.
- Порт 8000 занят → убейте процесс или измените порт в docker-compose.yml (например `8001:8000`).
- Ошибка миграций → выполните `docker-compose exec web python manage.py migrate`.

Удачной работы и приятной разработки! 🚀

Автор: stasevboris  
Дата создания: февраль 2026
```



